// we have n slots, ith slot has ai cans already and we have n buttons(we don't know which is for which slot)
// on pressing the button, 1 lemonade can comes out if available
// min number of button presses to get k cans of lemonade
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    ll testcases;
    cin >> testcases;

    for (ll testcase = 0; testcase < testcases; testcase++)
    {
        ll n, k;
        cin >> n >> k;

        vector<ll> a(n);
        for (ll i = 0; i < n; i++)
            cin >> a[i];
        
    }
}