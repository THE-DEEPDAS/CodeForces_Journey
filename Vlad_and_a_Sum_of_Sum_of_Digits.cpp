// we need to firstly find the sum of digits of all the numbers from 1 to n
// then do the sum of all that numbers  and print it
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    ll testcases;
    cin >> testcases;

    for(ll testcase = 0; testcase < testcases; testcase++){
        ll n;
        cin >> n;
        
        // 20 = 2, 21=3, 29 = 11 
        //100 = 1, 110 = 2, 120 = 3
        // so we can see a pattern
        // first find number of digits in n
        // according to number of digits in n, we can find the sum of digits of all the numbers from 1 to n
    }
}
